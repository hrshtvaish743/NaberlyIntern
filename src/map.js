/*var map;

function initMap() {
  console.log(document.getElementById('map').innerHTML);
  document.getElementById('map').innerHTML += "Hello";
  map = new google.maps.Map(document.getElementById('map'), {
    center: {
      lat: -34.397,
      lng: 150.644
    },
    zoom: 8
  });
  clearInterval(x);
}
var x = setInterval(initMap, 3000);*/
